# Backend URL — set this to your Render deployment URL in Vercel's environment variables
# Leave blank to use the localhost fallback defined in main.jsx
VITE_API_URL=https://your-backend.onrender.com
